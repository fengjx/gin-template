# 00 Brief

## 问题定义

- 系统配置后台目前只支持编辑已有配置项，无法新增配置。
- 配置值只有纯文本编辑体验，JSON 场景缺少结构化编辑和校验。
- `sys_options` 只有公开标记，没有上下线状态，无法在后台按运行时可用性管理配置。

## 目标

- 支持后台新增系统配置。
- 为配置项引入 `type` 与 `status` 元数据。
- 在 JSON 配置场景提供简单但可用的编辑体验与校验能力。

## 约束

- OpenAPI 仍是唯一契约源。
- SQLite 默认启动链路不能被破坏。
- 不引入重量级前端 JSON 编辑器依赖。
