# 70 Bug Log

## 记录

### BUG-1

- 症状：退出登录后浏览器仍保留 refresh cookie
- 复现步骤：登录 -> 退出 -> 检查 cookie
- 影响范围：用户可能被误判为仍可刷新登录态
- 根因：退出逻辑未按现有 cookie 参数完整覆盖删除
- 修复建议：统一复用发放 cookie 时的域、路径与过期策略进行删除
- 回归验证点：退出后 cookie 不存在，重新打开页面不会自动续期
